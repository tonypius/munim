# munim (classify)

The local-first transaction classifier at the heart of Munim: CSV in,
provenance-tagged categories out, powered by your own confirmed memory
rather than a cloud model.

See the repo root [README](../../README.md) for the full picture; this
package is `packages/classify/` in the
[munim uv workspace](../../docs/phases.md).
