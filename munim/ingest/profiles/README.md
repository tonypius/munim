# Shared bank CSV profiles

Community-contributed column mappings. Load with:
`munim import statement.csv --profile <name>`
(Wizard-created profiles are stored locally in ~/.munim; add them here
via PR to share.)
